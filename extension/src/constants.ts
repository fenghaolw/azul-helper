// Standard tile icon size used across all components for visual consistency
export const TILE_ICON_SIZE = 14;

// Responsive breakpoints for panel width
export const BREAKPOINTS = {
  COMPACT: 300, // Minimum width - single column
  MEDIUM: 450, // Medium width - 2 columns
  WIDE: 600, // Wide width - 3 columns possible
} as const;
